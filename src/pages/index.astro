---
---

<html lang="en-gb">
<head>
<meta charset="utf-8" />
<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
<meta name="viewport" content="width=device-width" />
<title>nhan.dev</title>
</head>
<body>
<main>
<div class="loader"><div></div><div></div><div></div></div>
<h1>building <a href="https://github.com/dnhn/nhan.dev" target="_blank" rel="noopener noreferrer">nhan.dev</a></h1>
</main>
</body>
</html>

<style>
  * {
    box-sizing: border-box;
  }

  html {
    color-scheme: dark;
  }

  body {
    margin: 0;
    padding: 0;
    background: #222 linear-gradient(0deg, #111, #222);
    text-align: center;
    font-family: monospace;
  }

  main {
    height: 100svh;
    padding: 2rem;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
  }

  h1 {
    font-size: 1rem;
  }

  a {
    background: linear-gradient(0deg, #333, white, #333) text;
    background-size: 100% 200%;
    color: transparent;
    font-size: 1.25rem;
    text-decoration: none;
    transition: background-position 500ms;

    &:not(:hover, :focus) {
      background-position-y: 100%;
    }
  }

  .loader {
    height: 12.5rem;
    padding: 2.25rem 1rem;
    display: flex;
    align-items: flex-end;
    gap: 1rem;
  }

  .loader div {
    width: 3rem;
    height: 3rem;
    background-color: #666;
    box-shadow: 0 4px 4px -1px black;
    filter: grayscale(1);
    animation-name: loader;
    animation-duration: 5s;
    animation-fill-mode: both;
    animation-iteration-count: infinite;
    animation-timing-function: ease-in-out;
  }

  .loader div:nth-child(1) {
    background-color: #c33;
  }

  .loader div:nth-child(2) {
    background-color: #3c3;
    animation-delay: .3s;
  }

  .loader div:nth-child(3) {
    background-color: #33c;
    animation-delay: .6s;
  }

  @keyframes loader {
    0% {
      transform: translateY(0);
    }

    50% {
      box-shadow: 0 10px 10px -1px black;
      filter: grayscale(0);
      transform: translateY(-150%) scale(1.125);
    }

    100% {
      transform: translateY(0);
    }
  }
</style>
